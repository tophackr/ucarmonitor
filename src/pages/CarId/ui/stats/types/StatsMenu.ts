export enum StatsMenu {
    cost = 'cost',
    list = 'list',
    fuel = 'fuel',
    parts = 'parts',
    tripCost = 'trip-cost'
}

export enum StatsGroup {
    stats = 'stats',
    calculated = 'calculated'
}
